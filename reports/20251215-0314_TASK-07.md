### タスク番号と目的
- TASK-07: 検索フィルタの実装、設定の永続化、UI整理で日常利用しやすくする。

### 実施内容・変更ファイル一覧
- 検索文字フィルタを実装（大文字小文字無視、スペース区切りAND）。フィルタ適用時に件数ログを表示。
- 設定値を `.local/settings.json` に保存/読込（StartRow/EndRow/検索文字、価格改定、待機、商品間待機、リトライ設定）。終了時自動保存、設定保存/読込ボタンも追加。読み込み失敗時はデフォルト復帰＋ログ。
- DataGrid のチェック列をテンプレート化し1クリックで切替。UIをグループ化（一覧取得・価格改定・待機・リトライ）。ログ欄拡張＆スクロール。
- 価格改定実行時に設定スナップショットを固定して実行中の変更混入を防止。
- RUNBOOKに検索フィルタ、settings.json の場所と復旧方法を追記。

変更ファイル:
- src/MercariMacroPriceTool.App/MainWindow.xaml
- src/MercariMacroPriceTool.App/MainWindow.xaml.cs
- src/MercariMacroPriceTool.App/ListingRow.cs
- docs/RUNBOOK.md
- reports/20251215-0314_TASK-07.md（本ファイル）

### 実行手順・コマンド、操作
- ビルド: `dotnet build MercariMacroPriceTool.sln`
- フィルタ: 検索文字を入力し「フィルタ適用」を押す（スペース区切りAND、大小無視）。
- 設定: 値を入力し「設定保存」で即時保存。終了時も自動保存。壊れた場合は `.local/settings.json` を削除して再起動。

### 動作確認結果（OK/NG）
- `dotnet build MercariMacroPriceTool.sln`: OK
- フィルタ/設定保存はUI操作前提で追加済み（実ブラウザを伴わない範囲）。

### 既知の課題と次の一手
- ブラウザを伴う実処理はログイン環境で要確認。
- runstate/settings の扱いは簡易実装。今後必要なら設定のバリデーションやUIでの復元確認ダイアログを追加検討。***
