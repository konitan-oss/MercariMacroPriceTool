### タスク番号と目的
- TASK-01: Playwright for .NET を導入し、Chrome を通常表示で起動して storageState によりログイン状態を維持する土台を作る。

### 実施内容・変更ファイル一覧
- Automation プロジェクトに Microsoft.Playwright を追加し、Chrome(headful/Channel=chrome) を起動するサービス `MercariAutomationService` を実装（storageState の保存・再利用含む）。
- ソリューションルート探索ヘルパーと URL 集約クラスを追加。
- WPF の「商品取得」ボタンから Automation を呼び出し、初回のみ手動ログインを促すダイアログを表示するハンドラを実装。
- storageState 保存先 `.local/storageState.json` を用意し、`.gitignore` に追加。
- docs を更新: Playwright 初回セットアップと手動ログイン手順を RUNBOOK に追記、SELECTORS に URLs を追加。

変更ファイル:
- .gitignore（新規）
- docs/RUNBOOK.md（更新）
- docs/SELECTORS.md（更新）
- src/MercariMacroPriceTool.App/MainWindow.xaml（更新）
- src/MercariMacroPriceTool.App/MainWindow.xaml.cs（更新）
- src/MercariMacroPriceTool.Automation/MercariMacroPriceTool.Automation.csproj（更新）
- src/MercariMacroPriceTool.Automation/AutomationEndpoints.cs（新規）
- src/MercariMacroPriceTool.Automation/SolutionPathLocator.cs（新規）
- src/MercariMacroPriceTool.Automation/MercariAutomationService.cs（新規）
- reports/20251215-0130_TASK-01.md（本ファイル）

### 実行手順・コマンド、操作
- パッケージ追加: `dotnet add src/MercariMacroPriceTool.Automation/MercariMacroPriceTool.Automation.csproj package Microsoft.Playwright --version 1.57.0`
- ビルド: `dotnet build MercariMacroPriceTool.sln`
- （参考/要手動）初回ブラウザ取得: `pwsh ./src/MercariMacroPriceTool.Automation/bin/Debug/net8.0/playwright.ps1 install chrome`

### 動作確認結果（OK/NG、再現手順）
- `dotnet build MercariMacroPriceTool.sln` : OK
- WPF アプリ起動＋「商品取得」押下による Chrome 表示と storageState 保存: 未実施（手動ログインを要するため、RUNBOOK の手順に従って確認してください）

### 既知の課題と次の一手
- 実処理（商品取得・価格改定）やログ出力、DB 永続化は未実装。次タスクで実装が必要。
- 初回起動時に Playwright のブラウザ取得（`playwright.ps1 install chrome`）と手動ログインが必要。`.local/storageState.json` が生成されることを確認する。
