### タスク番号と目的
- TASK-03: listings ページから商品一覧を取得し、指定範囲(StartRow〜EndRow)のうち公開停止中以外を WPF の表に表示する。

### 実施内容・変更ファイル一覧
- Automation に listings 取得処理を追加（スクロールでカードを読み込み、指定範囲を抽出。paused テキストは docs/SELECTORS.md を参照）。
- WPF UI に StartRow/EndRow/検索文字入力と DataGrid（チェック・商品名・価格・状態・RunCount・URL）を追加。商品取得ボタンで呼び出し、停止ボタンでキャンセル可能にした。
- DB RunCount を表示用に読込（ItemId があれば ItemStateRepository から取得、無ければ 0）。
- docs/SELECTORS.md を整備し PausedTextCandidates/URLs を明文化。RUNBOOK に商品取得の使い方を追記。

変更ファイル:
- docs/SELECTORS.md（更新）
- docs/RUNBOOK.md（更新）
- src/MercariMacroPriceTool.Automation/MercariAutomationService.cs（更新）
- src/MercariMacroPriceTool.Automation/ListingItem.cs（新規）
- src/MercariMacroPriceTool.Automation/SelectorsConfig.cs（新規）
- src/MercariMacroPriceTool.App/MainWindow.xaml（更新）
- src/MercariMacroPriceTool.App/MainWindow.xaml.cs（更新）
- src/MercariMacroPriceTool.App/ListingRow.cs（新規）
- reports/20251215-0215_TASK-03.md（本ファイル）

### 実行手順・コマンド、操作
- ビルド確認: `dotnet build MercariMacroPriceTool.sln`
- 商品取得手順（RUNBOOK に詳細記載）:
  1. `.local/storageState.json` があることを確認（未ログインの場合は「商品取得」押下後のブラウザで手動ログインし、保存）。
  2. StartRow/EndRow を設定（デフォルト 1〜500）。EndRow が大きいほど読み込みが遅くなるため必要に応じて縮小。
  3. 「商品取得」ボタンを押す。スクロールして EndRow まで読み込み、公開停止中でないものだけ表に表示。
  4. 途中で止めたい場合は「停止」を押下。

### 動作確認結果（OK/NG、再現手順）
- `dotnet build MercariMacroPriceTool.sln` : OK
- listings 取得〜DataGrid 表示: ログイン状態と実ページ DOM が必要なため手動確認前提。設定手順は RUNBOOK に追記済み。

### 既知の課題と次の一手
- DOM セレクタは heuristics ベース。実ページ変更時は docs/SELECTORS.md の PausedTextCandidates やセレクタを見直し、必要ならコードのセレクタ候補を追加する。
- 検索文字入力欄は将来用で未実装。必要ならフィルタロジックを追加する。
- RunCount は表示のみ。保存・更新ロジックは次タスクで拡張する。***
