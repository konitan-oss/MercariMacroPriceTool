### タスクと目的
- DataGrid のチェックボックスが1クリックで切り替わらない問題を修正し、チェック状態を対象選定に反映できるようにする。

### 実施内容・変更ファイル
- チェック用プロパティを `ListingRow.IsChecked` に統一し、INotifyPropertyChanged で通知。
- DataGrid のチェック列を DataGridTemplateColumn + CheckBox(TwoWay/PropertyChanged) に変更し、親 DataGrid の IsReadOnly を解除。
- 価格改定処理側の対象選定を IsChecked 参照に変更。

変更ファイル:
- src/MercariMacroPriceTool.App/ListingRow.cs
- src/MercariMacroPriceTool.App/MainWindow.xaml
- src/MercariMacroPriceTool.App/MainWindow.xaml.cs
- reports/20251215-0259_TASK-Checkbox.md（本ファイル）

### 実行手順・コマンド
- ビルド確認: `dotnet build MercariMacroPriceTool.sln`
- 動作確認: 商品取得後、DataGrid のチェック列を1クリックで ON/OFF でき、チェック状態が対象選定（価格改定）に反映されることを確認。

### 動作確認結果
- `dotnet build MercariMacroPriceTool.sln`: OK
- UIチェック切り替え: チェック列が1クリックで切り替わるよう修正済み（実ブラウザなしでUI確認前提）。

### 既知の課題・備考
- 価格改定処理はログイン/実ページが必要なため、実際のブラウザ環境で確認してください。
