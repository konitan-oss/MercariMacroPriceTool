### タスク番号と目的
- TASK-02: SQLite で Items テーブル相当を作り、ItemId 単位で BasePrice/RunCount/LastRunDate を保存・読込できるようにする。

### 実施内容・変更ファイル一覧
- Storage に Microsoft.Data.Sqlite を導入し、`.local/app.db` に Items テーブルを自動作成するリポジトリ `ItemStateRepository` を実装（GetByItemId/Upsert/UpdateRunCountIfNewDay）。
- Domain に Items テーブル対応モデル `ItemState` を追加。
- ストレージ用パス解決ヘルパーを追加し、`.local` 生成と DB パス決定を統一。
- WPF に「開発用: ダミー保存/読込」ボタンを追加し、ダミーデータの保存→RunCount 増加（新日付のみ）→読込結果を MessageBox 表示するフローを実装（再起動後も DB から読める想定）。
- docs/RUNBOOK に DB 保存場所と初期化説明を追記。.gitignore に `.local/app.db` を明記。

変更ファイル:
- .gitignore（更新）
- docs/RUNBOOK.md（更新）
- src/MercariMacroPriceTool.Domain/ItemState.cs（新規）
- src/MercariMacroPriceTool.Storage/MercariMacroPriceTool.Storage.csproj（更新）
- src/MercariMacroPriceTool.Storage/StoragePathProvider.cs（新規）
- src/MercariMacroPriceTool.Storage/ItemStateRepository.cs（新規）
- src/MercariMacroPriceTool.App/MainWindow.xaml（更新）
- src/MercariMacroPriceTool.App/MainWindow.xaml.cs（更新）
- reports/20251215-0201_TASK-02.md（本ファイル）

### 実行手順・コマンド、操作
- パッケージ追加: `dotnet add src/MercariMacroPriceTool.Storage/MercariMacroPriceTool.Storage.csproj package Microsoft.Data.Sqlite`
- ビルド: `dotnet build MercariMacroPriceTool.sln`
- （動作確認手順）WPF アプリ起動後、「開発用: ダミー保存/読込」ボタンを押下 → ダミーアイテムを保存し、RunCount を当日分だけ加算してから再読込し、結果をダイアログ表示。アプリを再起動しても同ボタンでデータが読めることを確認。

### 動作確認結果（OK/NG、再現手順）
- `dotnet build MercariMacroPriceTool.sln` : OK
- WPF でのダミー保存/読込（永続化確認）は手動確認前提。上記操作で `.local/app.db` に保存され、再起動後も読める想定。

### 既知の課題と次の一手
- 本番の商品取得・価格改定ロジック、ログ出力は未実装。
- WPF ボタンによるダミー操作は手動確認が必要。初回実行時に `.local/app.db` が生成される。***
